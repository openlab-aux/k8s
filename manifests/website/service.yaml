apiVersion: v1
kind: Service
metadata:
  labels:
    app: website
  name: website
spec:
  ports:
    - name: web
      port: 4231
      protocol: TCP
      targetPort: 4231
  selector:
    app: website
  type: ClusterIP
